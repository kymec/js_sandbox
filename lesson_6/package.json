{
  "name": "lesson_6",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel ./index.html",
    "build": "rm -rf dist && parcel build ./lesson_6/index.html --public-url /kymec",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "email-validator": "^2.0.4",
    "format-validator": "^0.9.0",
    "gh-pages": "^2.0.1",
    "random-email": "^1.0.3"
  },
  "devDependencies": {
    "cssnano": "^4.1.10"
  }
}
